# DEBT REGISTER — das bemalte Buch (R5, doc 45 H3)

Jede Stelle, an der ein Platzhalter steht oder ein Befund bewusst NICHT in seinem
Fund-PR behoben wurde, steht hier: Stelle · Zustand · erwartete Auflösung ·
Einbau-Schritt. Beim Eintreffen des Assets/Pakets wird nichts gesucht.
Angelegt in R5-P0; die Kritiker-Welle dieses PRs (5 adversariale Prüfer) hat
mehrere Einträge geliefert — Quelle je Eintrag vermerkt.

| # | Stelle | Zustand / Befund | Erwartete Auflösung | Einbau-Schritt | Quelle |
|---|--------|------------------|---------------------|----------------|--------|
| D-1 | p3-heft (Flyer über dem Swing, `ch01.level.json`) | Kein Engine-Bug: der Flyer hat KEIN Zielsystem (stürzt senkrecht bei Trigger \|Δx\|<24 px); sein Sweep-Band (x 528–608) überlappt den Swing-Bob (457–519) strukturell fast nie → „schießt immer zu weit rechts". Dazu B11: kein Grund, dort hinaufzuspringen. | P1-Level-Neuableitung entscheidet: versetzen (home c35→c33 gäbe 31 px Überlapp) oder ersetzen. | p3-Dossier → Grid-Neuschnitt | doc 45 A5 + P0-Analyse |
| D-2 | Cage-Skins ohne open-Kunst (`satchel`; Zensus-Korrektur: main trägt heute 1 Käfig [pencilcase], v2 plant 5 — s. dossiers-v2 README) | Ruhen für immer im Burst-Blatt (satchel_burst ist per Bildsichtung LEER — Klasse reproduziert dort nicht; nur Beat-Pose statt Ruhe-Pose). `pencilcase_open2` ungenutzt. | Art-Review aller Burst-/Open-Zellen; ggf. open-Paare nachkommissionieren (Codex) | anim.ts rendert automatisch, sobald `_open0`+`_open1` liegen (hasOpen) | R5-A8 + Kritiker |
| D-3 | Erwachens-Zeremonie × Kleckskammer (`sim.ts` freedCageIds) | Trip MITTEN in der Zeremonie (nach Burst, vor Runde 6, via „Später") remountet den Käfig GESCHLOSSEN (Kind wieder hinter Stäben, Runden verloren) — freedCageIds greift erst nach Runde 6. Erreichbarkeit der Bonus-Tür mid-ceremony ungeprüft. | Zeremonie-Zustand ins Shell-Ledger (wie freedCageIds) ODER Bonus-Tür während offener Zeremonie sperren | Folge-PR (P1/P3-Fenster) | Kritiker (A8-Prüfer) |
| D-4 | Redeemed-Zustand über den Bonus-Trip (`sim.ts` Remount) | Gelöste Wesen + restaurierte Dinge überleben den Trip NICHT (nur Käfige/Tips/Bücher haben Ledger) → die Tür ist ein bezahlter Reset-Knopf; XP evtl. doppelt verdienbar. Vorbestehende Klasse, von A2s Wortlaut (Position+Buchstaben) nicht gefordert. | Redeemed-Ledger nach dem freedCageIds-Muster für drained/Wesen | Folge-PR | Kritiker (A2-Prüfer) |
| D-5 | p9-Wiederbetreten (Preis vs. Rest-Inhalt) | Zweiter Kauf kostet volle 8 für einen (teil-)leergesammelten Raum (takenCells gelten auch in p9 — gewollt gegen Doppel-Farmen; der Preis weiß davon nichts). | **ERLEDIGT (B1, 2026-08-11) — Option A GEBAUT:** die Ledger-Anwendung ist jetzt BONUSRAUM-scoped (`sim.ts`: `phase === cfg.level.bonus`, nicht der literale id »p9« — dieselbe Regel trägt ch02s Kleckskammer, wie sie auch heißt). Rot-zuerst bewiesen (0 von 12 Buchstaben im zweiten Besuch), grün danach, und die Umkehr getampert (Ausnahme auf ALLE Phasen erzwungen → die p1-Zusicherung »eine geholte Zelle ist nie wieder holbar« wird rot) — der Guard trägt in beide Richtungen. Preis-Frage (voller Preis beim Zweitkauf) bleibt PK-R7-Ökonomie | GEBAUT in B1 | Kritiker (A2-Prüfer) · Koki-Nick 2026-08-11 |
| D-6 | Reachability-Restflächen (`level.ts`) | Nach der Kegel-Härtung dieses PRs verbleiben: Welten < 22 Spalten falsch-rot an der rechten Kante (kein min-width-Gesetz; kleinste shipped Breite 36) · Buchstaben weiterhin nur modell-bewiesen. | P1: pro Phase ein Tape mit `lettersGot === lettersTotal` (Vollsammel-Beweis) + min-width-Gesetz | P1-Paket (Grid-Neuschnitt nimmt die Tapes ohnehin neu auf) | R5-A7 + Kritiker |
| D-7 | Dev-Harness `solveTask()` (`PaintGame.tsx`) | Endlos-Rekursion auf einer Erwachens-Runde (resolveTask→onTask→resolveTask; umgeht offenbar die Hold-Queue). Dev-only. | Harness respektiert die Karten-Queue | Folge-PR | P0-Live-Lauf |
| D-8 | grids-v2-Spiegel (`docs/design/g1/paint/grids-v2/`) | Insgesamt gedriftet vom shipped Level (alte Copy, andere Arena, 2 fehlende `*` in p3 Reihe 21 — vorbestehend auf main); README-Behauptung „deep-equal" überholt. Die R5-Zeilen-Fixes wurden in allen drei Ablagen synchron gehalten. | P1 schneidet die Grids neu und definiert die Spiegel-Pflicht neu | **AUFGELÖST (R5-P1): Spiegel stillgelegt** — Kanon = Dossiers, Bau-Wahrheit = Level-File, Bindung = check:level-design (grids-v2/README) | P0 + Kritiker |
| D-9 | Mover-Attach ohne Lande-Beat (`sim.ts` Ride-Attach) | Kein `landed`-Event beim Andocken → keine Stauchung/kein Staub auf Movern (vorbestehend, kosmetisch). | Lande-Beat auch am Attach feuern | P1/Feel-Runde | Kritiker (A1-Prüfer) |
| D-10 | Hang/Vine frieren den Coyote-Zähler ein (`player.ts`) | Mini-Fläche: Kante → sofort Ledge-Grab → ↓-Abwurf zeigt bis ~5 Ticks Boden-Pose in der Luft (vom A1-Fix neu geöffnet, selten, geometrieabhängig). | **AUFGELÖST (R5-W1 F1):** nicht der Coyote wird genullt (das wäre Sprungverfügbarkeit = Physik = Bänder), sondern `poseGrace` — das eigene Gnadenfenster der ZEICHNUNG, das jeder Griff auf 0 setzt. Test: `player.test.ts` „Vine: über den Ranken-Kopf hinausklettern zeigt Luft"; vorher sechs Ticks »stand« in der Luft. | Folge-PR (mit D-9) | Kritiker (A1-Prüfer) |
| D-11 | E6 Spawn-Kollision Regel-Seite (p3) | Live reproduziert: p3 spawnt in die Regel-Seite, Karte feuert im ersten Tick. | Platzierung im P3-Regel-Seiten-Programm | P3-Paket (planmäßig) | doc 45 E6 |
| D-12 | Bild-vs-Grid hat kein Gesetz | Die Schein-Lücken-KLASSE wird nur manuell geprüft (`?grid=1`-Instrument); eine zweite Naht in p1/p2 finge kein Gate. | P1-Neuschnitt nutzt das Instrument; danach ggf. „drawn-footprint ⊇ claimed-cells"-Audit | P1-Paket | R5-A6 + Kritiker |
| D-13 | Fehlende Vokabel-Wesen: **Schere** (p2-Restore-Ersatz für das Pencil-Duplikat) + **Stuhl** (p3-Käfig-Insasse, B20-Karte) | Platzhalter: p2 behält obj_pencil bis Schere gemalt; obj_chair.png EXISTIERT statisch (dient als B20-Karten-Bild); es fehlen das Wesen-Paar (>=a/b) und die Grau-Silhouette am Käfig | Codex Batch aq: `obj_scissors` (a/burst-Zellen) + `chair`-Wesen (≥a/b) | p2/p3-Grid: Skin-Tausch nach Review; P2-Copy nennt den Stuhl | R5-P1 Dossiers |
| D-14 | Farbvarianten (B9): Kleber orange-rot, Spitzer blau-metallic, Käfig-Ranzen farblich getrennt vom Stampfer/Schaukel-Ranzen | Bestand ist braun/gelb-lastig — Kokis „nicht alles braun" | Codex Batch aq: Regenerationen | Stem-Austausch, IDs unverändert | R5-P1 p3 §7 |
| D-15 | `regelseite` + `bonusbuch` sind ENGINE-GEZEICHNETE Allowlist-Fallbacks (befristet bis 2026-09-30) | In der AAA-Runde ein Selbstwiderspruch: zwei sichtbare Sammel-Klassen ohne gemalte Kunst | Codex Batch aq: gemalte Regel-Seite (Schatz-Design, doc 45 E1!) + Bonus-Buch | Stems einhängen, Allowlist-Einträge löschen | R5-P1 Kritiker (Bau) |
| D-16 | B2-Kanten-Kit + B12-Zustands-Zellen (Heft „flattert wütend", Ranzen „klappert") + Terrassen-Kappen (B3-Ersatz) | Plattform-Ränder sind „Frankenstein"-Kanten; Karten-Fiktion ist in der Welt unsichtbar | Codex Batch aq | mass.ts-Kanten-Verdrahtung (Kit) · anim-Zustandszellen | R5-P1 p3 §8 |
| D-17 | Entity-Kontakt-Knockback ist ein No-op (`sim.ts` onEntityEvent "encounter" verwirft den Rückgabewert von applyKnockback) | Vorbestehende Engine-Auffälligkeit, Kritiker-Runde-2-Beifang — Encounter wirken ohne physischen Rückstoß | **GEMESSEN, ENTSCHEIDUNG OFFEN (R5-W1 F1):** kein No-op mehr *und* das Vorzeichen war verkehrt (hätte ins Wesen hinein gestoßen). Fix geschrieben und grün, aber NICHT gemergt: p2 löst zwölf Begegnungen pro Durchlauf aus, mit echtem Rückstoß erreicht der Pilot den Ausgang nicht mehr (2361 statt 965 Ticks; Gegenprobe ohne Fix: sauber). Kokis Frage: jede Wesen-Berührung, oder nur die Kreide des Bosses? Tests liegen fertig in `hit-knockback.test.ts` (`describe.skip`). | Folge-PR (mit D-9/D-10) | R5-P1 Kritiker (Bau) |
| D-18 | p1-Audio + Käfig-#1-Insasse SOUND SYSTEM | Die »stumme Halle«-Raumidee braucht den deklarierten Audio-Zustand (still → Musik-Zündung bei Befreiung); das Sound-System-Wesen hat keine Kunst | Codex: sound-system-Wesen (≥a/b) · Engine: Phase-Audio-Flag | p1-Grid + P3-Audio-Fenster | R5-P1 Welle (p1) |
| D-19 | p2-Kunst: Lampen-Korridor-Kit · Konfiskations-Pult-Ensemble · Tablet (Käfig-#2-Insasse) | Der Lampen-Korridor (B10-Heimat) und Merles Konfiskations-Pult sind neue Setzstücke ohne Stems; tablet-Wesen fehlt | Codex Batch aq | p2-Grid: Platzhalter aus Bestand (plank/prop), Tausch nach Review | R5-P1 Welle (p2) |
| D-20 | ~~p9: `seal_sticker` nie gerendert~~ **ERLEDIGT (R5-W1 · D2)** | Der PERFEKT-Sticker lag als Stem auf der Platte, während die Karte „Klecks stempelt dir einen Sticker auf die Karte" versprach und eine code-gezeichnete Rosette zeigte — ein Bild, das ein Versprechen ersetzt, ist die Buchstaben-Ehrlichkeit mit Pixeln statt Zahlen | **Verdrahtet im `bonusend`-PERFEKT-Zweig** (`art.seal_sticker`, 84 px, leicht gekippt); Rückfall auf die Rosette, wo das Blatt fehlt (keen-art) | — | R5-W1 · D2 |
| D-21 | Arena: `obj_picture` fehlt (Käfig-#5-Insasse PICTURE, B20-Karte ohne Bild) · Saal-Sieg-Zustand (Foto lehnt farbig, warmes Licht) · guardian-Größen-Drift (Test-Kommentar 52 px vs. PaintScene 68 px) | Der Foto-Beat braucht Karten-Bild + Welt-Antwort; die Test-Kommentar-Drift ist Register-Beifang | Codex: obj_picture + Sieg-Zustands-Zellen · Test-Kommentar fixen | Arena-Grid (Platzhalter satchel+Licht) + P4-Schnittstelle | R5-P1 Welle (arena) |
| D-22 | p1-Props: Schwarzes-Brett, Spind-Gesichter, Alkoven-Rahmen · Radierer-Bounce-Feel (Instanz-Tuning-Wunsch) | Neue p1-Setzstücke ohne Stems; der Hüpfer liest sich als Rhythmus schwach (Apex ~5 px) | Codex Batch aq (Props bleiben offen) · **Bounce-Feel ERLEDIGT (R5-W1 F1):** kontakt-getaktete Schwerkraft, gemessen Apex 3,6 → 10,4 px und Periode 6 → 13 Ticks (gleichmäßig). Achtung: die Zeile „Apex ~5 px" und `ch01-dossiers-v2/p1.md:113` beschreiben ab jetzt den alten Stand. | p1-Grid: Bestand-Platzhalter | R5-P1 Welle (p1) |
| D-23 | p9 Tape B (Timeout-Pfad) + Rückkehr-Band (A2 auf Band: Tür-Spawn, Börse, Wirts-Buchstaben) | p9.md §10 verlangt BEIDE Tapes durch die Rückkehr; das Proof-Schema trägt ein Tape je Phase, und das Zweitbesuchs-Staging ist durch D-5=A ENTSCHIEDEN (Respawn — Tape B braucht keinen geseedeten Teilstand mehr, nur die Schema-Erweiterung) | Proof-Schema um Zweit-Tape erweitern + aufnehmen; **D-5 ist seit B1 nicht nur genickt, sondern GEBAUT — Tape B braucht keinerlei geseedeten Teilstand mehr, nur noch die Schema-Erweiterung (ein Tape je Phase reicht heute nicht für zwei p9-Läufe)**; bis dahin decken die R5-A2-Sim-Tests die Rückkehr-Semantik, inkl. des neuen Zweitbesuchs-Tests | Tape A (12/12 PERFEKT) ist an Bord | R5-P1 Grid-Schnitt |
| D-24 | p2 Rettungs-Karte TABLET: Dossier-Wunsch `spell` vs. ch01-Palette [choice·wheel·restore·oddone] (doc 41 §1) | Das Karten-Gate verbietet spell in ch01; die Karte fährt als choice (»It's a tablet«) | P2-Skript-Paket entscheidet: Palette erweitern ODER Dossier-Zeile amendieren | Karte ist live als choice, Korpus-verifiziert | R5-P1 Karten-Gate |
| D-25 | p3 Tinten-Querung: Fähre `p3-ruler` + Schaukel `p3-swing1` INTERIM AUSGEBAUT (`ch01.level.json`, p3.md §3/§10) | Koki-Entscheid 2026-08-11 (Replay): beide Mover »lesen sich als Durchfallen« — der Ride-Pose-/Attach-Defekt (A1, D-9) macht die getragene Ruhe unlesbar. Ersatz = zwei statische Sprung-Planken (r17 c32–33 · c36–37, drei Δc3-Sprünge über je 2 Spalten Tinte); E·S·T von (33,16)(35,16)(37,16) → (30,16)(34,16)(38,16), Bonus-Buch (29,10) → (26,12). **Gemessener Grund, warum das kein Parameter-Tweak war: eingefroren (`dxTiles:0`) brechen NEUN Gesetze** — die Fähre war die einzige Brücke. PREIS der Zwischenlösung, deklariert: die Fahr-Lektion »WAIT — THEN STEP!« existiert nicht mehr (Screen 2 ist jetzt Geschick statt getragener Ruhe) · der Mut-Preis des Buchs sinkt von Tinten-Dunk (~7–10 s) auf Pier-Wiederaufstieg (~1 s) · die Vokabel `ruler` hat kein Objekt mehr im Raum (die Abdeckungs-Regel fängt das NICHT, weil `ruler` als `architecture` klassifiziert ist — eine Ehrlichkeits-Drift, kein Gate-Fehler) | Ride-Animation reparieren (Pose beim Attach + Lande-Beat, s. D-9/A1), dann Fähre (30,17) dx9 per390 und Schaukel (30,11) ropePx40 per200 zurücksetzen, die zwei Platten wieder entfernen, E·S·T und Buch auf die alten Anker drehen, p3-Tape neu aufnehmen. Mit dem Rückbau gehört D-1 mitentschieden (der Flatterer-Sweep überlappt den Schaukel-Bob strukturell nie) | Rückbau = Level-Diff (2 Reihen + 5 Anker) + eine Tape-Aufnahme; KEIN Engine-Ausbau — die Mover-Maschinerie bleibt vollständig verdrahtet und getestet (`ride-pose.test.ts`, `entities.test.ts`-Plattform-Block, `level.ts` `platformSweepCells`), damit die Fähre ein Zuhause hat, in das sie zurückkommt | Koki-Nick 2026-08-11 (Replay) + B1-Messung (checkLevelLaws: 9 Verstöße bei eingefrorenen Movern) |
| D-26 | Kurs-Blätter `crust_p3_a` / `_b` (262 px hoch) | **Gitter-Verrastung, live von Audit 10 gemessen:** 544/262 = **2.08 Zellen** Periode — praktisch genau zwei, also sitzt die Wiederholung des Gemäldes für immer in Phase mit dem Zellgitter. Die Engine kann es NICHT heilen: ein Kurs muss `CRUST_H` exakt füllen, sein Maßstab ist keine Wahl (die Innenmasse wird per `bodyScaleOf` weggeschoben, der Kurs nicht). | Blätter auf **212 px Höhe** neu schneiden (Gesetz: 544/H ∈ [2.15, 2.85], nie ±0.08 an einer ganzen Zahl) | Codex AS · SPEC_MASSEN_KIT §2. Bis dahin meldet Audit 10 ⚠ statt Fehler | R5-W1 A1 · Audit 10 |
| D-27 | Unterseiten-Kante (`edgeD`) — **existiert überhaupt nicht** | Wo eine Masse eine Decke oder einen Überhang bildet, endet sie an einem rohen waagerechten Schnitt. `planMass` kennt edgeL/edgeR/4 Ecken — keine Unterseite. Ein neues Kit-Feld ist blockiert, solange kein Blatt liegt: `massStems` → `check-paint-art` fällt hart auf ein fehlendes PNG. | Codex AS liefert die Zellen; ODER engine-gezeichneter Kontaktschatten nach dem `planPlatformShadows`-Muster (kostet keine Kommission) | SPEC_MASSEN_KIT §5, Zellen 2/3 | R5-W1 A1 |
| D-28 | `mass_sediment.png` ist als GEMÄLDE ein Loch | Gemessen **4.8 % mittlere Leuchtdichte**. Die R5-W1-Tiefenrampe drückte den Anteil der unter 6 % gezeichneten Innenzellen von 48.2 % auf 0.9 %, indem sie das Blatt fast überall vermeidet — aber wo es doch gezeichnet wird, bleibt es schwarz. Zwei blinde Prüfer unabhängig: »die dunkelste Materie der Referenz ist immer noch erkennbar blaues Holz, nicht Schwarz«. | **ENTSCHIEDEN (Koki, 2026-08-11): Option A — AUFHELLEN.** Codex AS: Tiefenpapier bei 12–18 %, Sediment bei 7–10 % MIT Farbton (Sättigung ≥ 12 %). Die Werte sind damit Vorgabe, nicht Vorschlag. | SPEC_MASSEN_KIT §4 (Ruling im Kopf des Abschnitts) | R5-W1 A1 · Kritiker-Runde 1 · Koki-Nick 2026-08-11 |
| D-29 | Eine Innenmasse für alle fünf Phasen (`sharedMass`) | Die Nachtklasse trägt dieselben Bücher wie die Morgenhalle; Phasen-Identität lebt nur im Kurs und in der Wäsche. Ein Phasen-Tint wäre möglich, macht aber Audit 1 schal (es misst L3 aus den Quell-PNGs, nicht aus dem Render) — die ehrliche Lösung ist Malerei. | Codex AS: ein `mass_body_<phase>`-Blatt je Raum | SPEC_MASSEN_KIT §3 | R5-W1 A1 |
| D-30 | `setpiece`-Massenart — von doc 36 §2 v1.2 (2026-07-28) VERLANGT, nie gebaut | Die Amendment stellt gekachelte Montage für besondere Formationen ausdrücklich still und nennt den Engine-Anschluss (»`setpiece` mass kind mit Stem + Span«). In `mass.ts` gibt es die Art nicht. Bewusst NICHT in A1 gebaut: ein Anschluss für Kunst, die nicht kommt, ist toter Code. | Zusammen mit dem ersten Setzstück-Blatt bauen | SPEC_MASSEN_KIT §6 · Kandidaten: p3-Rutschenauslauf, p4-Podeste, p1-Kellerschacht | R5-W1 A1 |
| D-31 | Zwei Tests flackern am 5-s-Standard-Timeout | `pickups.test.ts` »the shipped chapter passes it« und `content-levels.test.ts` laufen einzeln in 3.6–3.9 s, unter paralleler Last aber über 5 s. **VORBESTEHEND, bewiesen:** derselbe Lauf auf unverändertem `origin/main` fällt identisch (637/639); der A1-Branch steht bei 650/652 — dieselben zwei, plus 13 neue grüne. | **ERLEDIGT von B1 (#259), noch bevor dieser Eintrag gehoben wurde:** `packages/game-paint/vitest.config.ts` (neu) setzt `testTimeout`/`hookTimeout` auf 120 s — aus Messung begründet, nicht aus Geschmack, samt der eigentlichen Ursache (das Erreichbarkeits-Gesetz startet je Knoten eine Wegsuche). Beim Rebase von A1 auf B1+E1 am 2026-08-11 lief die Suite **679/679 grün**, diese beiden inbegriffen. | GEBAUT in B1 (#259) | R5-W1 A1 (Beifang) · beim A1-Rebase 2026-08-11 als erledigt nachgewiesen |
| D-32 | VRAM: jede `TileSprite` legt eine EIGENE POT-Leinwand + GL-Textur an | Bei 512² ≈ 1 MB je Stück. Nach A1 plant p2 **323 gekachelte Stücke** (vorher 181) — grob 300 MB Texturspeicher für das Terrain EINER Phase. Vorbestehende Klasse, von A1 verschärft (die Tiefenrampe teilt Läufe je Tiefen-Bucket). | Vertikales Zusammenfassen der Innenläufe zu Rechtecken + EIN Graphics-Verlauf je Massen-Region (Muster: `planBandShade`) — oder ein gemeinsamer Textur-Atlas | **An Session E1 (Performance-Audit) gefiled** — dort messen, hier nicht raten | R5-W1 A1 · Design-Review C.4 |
| D-33 | Runde »Bokeh«-Scheiben über dem Terrain | Beide Kritiker-Runden nannten sie unabhängig: halbtransparente graue Kreise liegen über der Masse, unabhängig von Tiefe und Material — »wie aufgeklebte Abziehbilder über einer Leere«. Sie stammen aus der Luft-/Motes-Schicht, nicht aus dem Terrain. Vorbestehend, in BEIDEN Frames identisch. | Entscheiden: bewusste Licht-Pfützen (dann an die Tiefe koppeln) oder streichen | Luft-Runde / E1 | R5-W1 A1 · Kritiker beide Runden |
| D-34 | Käfig-Insassen sind unsichtbar (`p1-cage1`, `p2-cage-tablet`, `p3-cage4`, `p4-cage5`) | Die vier Objekt-Käfige zeigen nur Gitter + Licht; der Insasse hat keine Zelle. Die Rettungs-Karten NENNEN ihn deshalb (deutscher Rahmen, englische Antwort) — ohne den Namen wäre die Karte unlösbar (Kritiker-Fund C1). Die Zeile ist damit fair, aber noch nicht ganz wahr. | Codex AQ »Käfig-Insassen« (durch Kokis Tor 1 entsperrt) | Wenn die Blätter liegen: `showsDe` der vier `rsc.*`-Karten auf das Sichtbare heben (»Hinter den Gittern steht der Lautsprecher«) | R5-C1 |
| D-35 | Käfig-WACKELN als Copy-Deckung (`rsc.soundsystem.r1`) | Die Zeile »Der Käfig wackelt« ist in C1 gestrichen, weil der `shaking`-Zustand in ch01 nur über einen Faust-Treffer erreichbar ist (`entities.ts:870`) — also nie. Session F1 baut ein lesbares Wackeln auf der Hülle. | **BEDINGUNG ERFÜLLT: F1 ist gemergt (#257, 2026-08-11).** `anim.cageBreath` liegt in `renderEntities` und greift für JEDEN Käfig, der weder erlöst noch verborgen ist — der Ausschlag ist dort in Pixeln definiert, nicht mehr als 1,5-px-Drehung. Die Bewegungs-Behauptung darf damit zurück; die Formulierung ist eine Copy-Entscheidung und gehört ins P2-Skript-Fenster, nicht in einen Rebase. | Eine Zeile in `ch01.tasks.v2.json`, gegen den STORY_SPINE §4 geprüft | R5-C1 · beim C1-Rebase 2026-08-11 als entsperrt nachgewiesen |
| D-36 | `hints.deDesc` / `hints.deWord` sind von KEINEM Gate gedeckelt | `MAX_LINE_DE` deckt nur `showsDe`/`storyDe`/`colourAskDe`; `enc.ranzen.q1.deDesc` trägt 59 Zeichen und ist grün. Der Spine schreibt »eine Zeile« vor, die Maschine prüft es nicht. | Länge (und Register) in `check-game-tasks.mjs` auf die Hinweis-Felder ausdehnen — eigener PR, weil er Bestand rot färben kann | Layer 6 der Prüfliste erweitern | R5-C1 (Kritiker-Fund) |
| D-37 | Bilanz-Seite + Boss-Konsolen-Karte NICHT live nachgespielt | Beide Copy-Stellen sind umgeschrieben, typgeprüft und vom `retired-phrase`-Gesetz gedeckt, aber der Browser-Durchlauf endete am Boss: `__domigoPaint.solveTask()` hängt auf einer `order`-Karte (Klasse D-7). | Nach D-7s Fix je ein Screenshot; bis dahin Kokis 30-Sekunden-Blick am Kapitelende | Report C1 §»Was ich NICHT verifiziert habe« | R5-C1 |
| D-33 | Codex-Lieferweg: AQ/AQ2 lagen NICHT unter `~/Code/codex-art-lab/batch-*` (dem Pfad, den alle `import-batch-*.mjs` lesen), sondern unter `~/Code/codex-lab/01_PROJECTS/domigo/codex-work/batch-*` | A2 hat sie zunächst als »nicht geliefert« befundet, weil der kommissionierte Pfad leer war. Der Lab-zu-Art-Lab-Pull ist in den Kommissionen benannt, aber nirgends als SCHRITT festgehalten. | Den Pull-Schritt in die Kommissions-Vorlage schreiben ODER `LAB` in den Importern auf beide Pfade schauen lassen | Folge-PR (eine Zeile im Importer) | R5-W1 A2 |
| D-34 | `farb_audit` repariert nur die `_a`-Zelle von vier Wesen, die je 3–5 Zustands-Zellen haben (pencil · eraser · ranzen · satchel) | Die Geschwister-Zellen (`_act` `_b` `_dazed` `_run` `_stomp` `_telegraph` `_burst` `_shake`) stammen aus früheren Batches und tragen die ALTEN Farben. Nur `_a` zu tauschen ergäbe ein Wesen, das beim Losrennen die Farbe wechselt. **Deshalb in A2 NICHT getauscht** — nur die drei Ein-Zellen-Wesen (Kleber · Spitzer · Fund-Schultasche) sind neu. | Nachkommission: die vollen Zustands-Sätze der vier in den neuen Farbwelten | Codex AQ4 (oder AS-Anhang) | R5-W1 A2 |
| D-35 | `cage_insassen` Reihe 2 liefert VIER KOMPLETTE NEUE KÄFIGE (Weidenkorb + Vorhängeschloss) statt der kommissionierten Grau-Silhouette hinter den Gittern der BESTEHENDEN satchel-Hülle; `farb_audit` Zelle 2 wiederholt denselben erfundenen Käfig | Verdrahten würde ch01s Käfig-Design still ersetzen — Käfig-Gesetz + B20-Portrait-Gesetz sind beide Koki-Tore. Reihe 1 (die vier Insassen-Portraits) ist importiert; Reihe 2 liegt unangetastet. | Architekt/Koki entscheiden: neues Käfig-Design annehmen ODER Silhouetten-Layer nachfordern | Nach dem Tor | R5-W1 A2 · Sicht-Review |
| D-36 | `tip_treasure.png` (AQ2) — **ABGELEHNT** | Blind-Review: der Glüh-Verlauf liegt farblich so nah an `#FF00FF`, dass JEDE der vier Zellen den Schlüssel-Gate reißt (1170–2288 Randpixel je Zelle gegen 0–611 bei den sauberen Blättern); Übergänge über ~40 px, die den Magenta-Bereich nie verlassen. Außerdem zeigen nur 2 von 4 Zellen überhaupt die Regel-Seite. | Neu rendern mit einer Glüh-Rampe klar weg von Magenta (Gold/Weiß statt Pink-Mitteltönen) | Codex AQ4 | R5-W1 A2 · Blind-Review |
| D-37 | `cage_wobble.png` (AQ2) Bild 2 — Loch im Objekt | Blind-Review: ein ~120 px langer, fast reiner Magenta-Streifen läuft MITTEN durch eine massive Holzstrebe (in Bild 1 ist dieselbe Strebe durchgehend gemalt). Unter dem Schlüssel-Import bekäme das Bild ein Loch durch einen soliden Balken. Die anderen drei Bilder + die Wackel-Bewegung sind sauber (MAD 41/40/29). | Bild 2 neu rendern | Codex AQ4 | R5-W1 A2 · Blind-Review |
| D-38 | `p1_requisiten.png` (AQ2… AQ Blatt 6) — ACCEPT WITH FIX, daher NICHT importiert | Blind-Review: die HALTE-Kritzel-Zelle trägt den Kreide-Bogen, aber **kein »HOLD!«** (namentlich kommissioniert); nur 3 von 5 Spind-Türen haben das müde Gesicht, das für ALLE gefordert war; die Noten-Wirbel haben denselben weichen Magenta-Saum wie das abgelehnte Blatt (lokal begrenzt). | Nachbessern, dann importieren | Codex AQ4 | R5-W1 A2 · Blind-Review |
| D-39 | Vier importierte Stems werden von nichts geladen (`regelseite_open` · `obj_soundsystem` · `obj_tablet` · `obj_picture`) | Sie liegen bewusst auf der Platte: die Lese-Karte (D1-Lane) und die B20-Insassen-Karten (Koki-Tor) verdrahten sie. E1s neue Warnung »painted stems loaded by nothing« zählt sie ab jetzt mit — das ist erwartet, nicht neu entstanden. | Mit der Lese-Karte bzw. dem B20-Delta verdrahten | D1 / B20-Tor | R5-W1 A2 |
| D-40 | **SCHLEIFEN-EHRLICHKEIT**: vier von acht `*_loop`-Stems waren keine Schleifen | Gefunden beim Beweis der Tinte IM SPIEL: `pool_ink_loop.png` ist 512×512, das Gemälde darin nur **397×160** — 78 % der Fläche leer. Als Kachel gezeichnet werden aus den Seitenrändern LÜCKEN (die Tintenfläche las als Reihe einzelner Kleckse) und aus den oberen/unteren Rändern ein 5-px-Motiv. Klasse, nicht Einzelfall: `pool_ink_loop` · `spikes_nibs_loop` · `strip_ground_loop` · `strip_ice_loop` — **alle vier sind 512er-Zellblätter**, die vier ehrlichen sind allesamt 2048er-Bandblätter. Der Name sagt „loop", das Format sagt „Zelle". | **REPARIERT (A3):** `scripts/trim-loop-margins.mjs` schneidet die durchsichtigen Ränder weg (dieselbe Rolle wie `strip-key-fringe.mjs`), idempotent, `--dry`. Blind gemessen: Wiederhol-Korrelation 0,81 bei 48 px (= 1 Zelle) → 0,51 bei ~119 px; Lücken-Anteil 22,3 % → 3,7 %. | Erste Fassung des Skripts schnitt AUCH die vier gesunden Bandblätter (3–13 % kleiner) — der Renderer leitet ihren Maßstab aus der QUELLHÖHE ab. Regel verengt: nur Blätter mit SEITEN-Rändern werden geschnitten. | R5-A3 |
| D-41 | Kein Gate verbietet ein `*_loop`, das nicht schleift | Der Defekt in D-40 lag seit Monaten live und kein Prüfer konnte ihn sehen: `check-paint-art` prüft Existenz + Magenta-Saum, nicht Kachelbarkeit. | Gate nachziehen: ein als Band gezeichneter Stem darf keine durchsichtigen Seitenränder haben (und sollte passende Enden haben — CP-12). `trim-loop-margins --dry` ist bereits die Messung; sie muss nur hart fallen. | Folge-PR (klein) | R5-A3 |
| D-42 | Tinten-Körper ist gebändert, nicht verlaufend | Blinder Kritiker: »2 unique RGB values across 63.000 sampled pixels — the client's original complaint relocated one layer down«. Die Tiefenrampe war über SECHS Reihen gespreizt, ch01s Tinte ist VIER tief — der Teich erreichte nur das erste Drittel seines eigenen Abfalls. **In A3 auf 3 Reihen/0,42 verengt**, damit die Rampe im echten Teich fertig wird. Es liest jetzt als Schichten statt als Platte, aber weil die Rampe pro ZEILE (16 px) arbeitet, ist es eine Treppe, kein Verlauf. | Verlaufs-Füllung über den Teich (Muster: `planBandShade`) statt Tint pro Zeile | Folge-PR / Luft-Runde | R5-A3 · Kritiker |
| D-43 | Tinte ist chromatisch nicht »Tinte« | Blinder Kritiker: Körper-Farbton **H220°** gegen Himmel **H227°** — die Gefahr hat fast denselben Ton wie der Hintergrund, und die Glanzlichter lesen als vergoldetes Metall statt als öliger Tinten-Schimmer. | Palette der Tinte gegen ihren Raum absetzen (Kunst ODER ein Tint auf dem Band) | Folge-PR | R5-A3 · Kritiker |
| D-44 | p3-Balustrade: die stärkste falsche Affordanz im Spiel — **NICHT gebaut** | Live bestätigt (Screenshot `a3_p3_balustrade.png`): die Hintergrund-Balustrade rechts sitzt auf **y≈545**, die begehbare Bücher-Terrasse links auf **y≈540** — dieselbe Höhe, also liest der Hintergrund als Fortsetzung des Wegs. Sie ist in `l2_p3` EINGEMALT; das Band zu verschieben verschiebt auch die Hecke und rührt an Audit 8. | Kunst: die Balustrade aus dem Band herausnehmen oder auf eine andere Höhe malen | Codex (mit AQ4/AS) | R5-A3 · B1-Kritiker |
| D-45 | Krakel-Marker-Eigenkontrast — **NICHT gebaut** | B1s Kritiker: der Checkpoint-Marker hat in den hellen Leveln keinen Eigenkontrast und wurde beim Banking in p1 vom Spieler komplett verdeckt. In A3 nicht angefasst (Budget). | Konturschema wie `heroEdgeFor` auf den Marker anwenden | Folge-PR (klein) | R5-A3 · B1-Kritiker |
