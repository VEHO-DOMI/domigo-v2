# CODEX DRAFT — NOT CANON

## Correct class-photo card and final blind packet

The final chapter-one rescue card now declares photo_frame_cage_a. CardShell recognises exactly that portrait together with captive=picture and no person flag, uses CLASS_PHOTO_WINDOW and the complete klassenfoto_a as its foreground-frame occupant, and renders its plate at 210 px high instead of 132 px. The new class photo is shown at its original aspect ratio, without an old satchel, object placeholder or image cutout. The existing devices and Merle keep their own registered windows and default size. No scene, animation or window-registration edits in this lane.

The real source frame is 768×674 and the class photo 1408×972. The inner proportional image remains inside x163…593 and ends at y465. The frame has two continuous foreground bars, measured at x243…264 and x495…516. These bars intentionally cover portions of the photo; every pixel of the contained image outside those two bars is transparent in the overlying frame. The first geometry test incorrectly required a completely clear opening and was red (13,024 foreground-bar pixels). After direct visual inspection and column measurement, its assertion was corrected to the actual two-bar contract, including zero unexpected blockage and positive bar/clear areas. This is not an image change or threshold reduction. Both original red log and final tests remain outside the student packet.

## Gate amendment and deliberate failure

The alternate portrait is accepted only for a chapter-one rescue/choice/state-it card, closed photo_frame_cage_a, exactly satchel skin, and exactly one real serving entity in the authored phases. That entity must be a cage, shellArt=photo_frame_cage, captive=picture, no classmate. The authored answer must be It's a picture. and the taught exercise g1u01.w.picture. The actual klassenfoto_a artwork must exist. A separate fourteen-case selftest tests the real positive and missing/wrong frame, captive, role, person, phase, ambiguous or duplicated route, wrong answer/word, open frame, other chapter and missing photo. All pass, beside all sixteen existing device cases. The deliberate lab-only gate mutation removing photo shell agreement exits 1 specifically because the missing-frame and device-frame negatives stop biting. The actual gate was never weakened by this mutation.

Full check-game-tasks exits 0: 199 tasks across six files green. Three relevant test files pass 29 tests: actual photo registration/emission plus all existing device/Merle window and portrait tests. Logs: blind-solve/photo-gate-full.log, photo-gate-selftest.log, photo-gate-tamper.log and photo-tests.log.

## Packet release

student/061-01.png and its actual rendered text were regenerated through the real CardHost. The other 102 PNGs are byte-identical to the first packet; first sixty full task/context records also remain identical. The only authored task delta is the last stimulus.art value. The private task source hash, last-card arena context, root mapping and output manifest were updated together. Complete proof: blind-solve/photo-packet-delta-proof.json. There are still exactly 61 tasks, 103 PNG screenshots and one student text list. Student filenames contain only sequential numbers; no task IDs, keys, evidence or grounding metadata leak into the text list. Give blind readers only blind-solve/student/. The old photo screenshot, keys and driver remain outside it.

Final package typecheck also exits 0 (blind-solve/photo-typecheck.log).
