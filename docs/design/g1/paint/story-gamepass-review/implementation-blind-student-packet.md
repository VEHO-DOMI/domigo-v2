# CODEX DRAFT — NOT CANON

## Original packet record (historical; see final addendum below)

Give readers ONLY `blind-solve/student/`. It contains 103 numbered screenshots and `ANSICHTEN.md`, the text read from the rendered cards. No answer keys, task IDs, source JSON, evidence/grounding fields, HTML bundle or correctness markers are in this directory. Every English option is retained, including the correct option among its alternatives. No answer choices were solved or judged for this report.

Coverage: all 61 current chapter-one cards. The 61 opening views are supplemented by 10 actual second colour steps, 8 individually revealed Memory positions, and 24 further wheel positions. Distribution: 32 choice, 10 restore, 8 oddone, 4 wheel, 3 mistake, 2 order, 1 memory, 1 typed. There are no match cards in this current authored set. Each Memory view starts again from the same fresh task and reveals only one position; no matched-pair state is furnished. Wheel views use the real up/down control and never submit a choice.

The isolated browser renders the actual `CardHost`, `CardShell`, each actual control component, the existing card stylesheet and the three shipped local fonts. Image paths are the real current chapter/hero artwork; captive and round context come from the real level. All 61 stimuli are entity portraits, so no scene snapshot was fabricated. The ten second restore steps are reached by clicking the actual naming button in the running CardHost, not by injecting a colour state. The private driver reads the name key only for this navigation. All resulting second-step screenshots contain only the actual child-facing colour question and choices.

Browser profile: isolated headless Chrome; 1100×1000 CSS pixel viewport, device scale 1, supported reduced-motion preference. Reduced motion also disables the timer normally in CardHost, so the reading packet neither times out nor loses a card during capture. Each screenshot crops the real card bounding rectangle with a 16-pixel outside margin. Every view passed image-loaded and no-scroll-overflow assertions before capture. Screenshots were visually sampled for device containment, neutral-colour overlay, order, Memory and photo. The first driver attempt stopped on a quoting typo in its wheel-button selector after card 30; this affected only the lab capture driver. The corrected complete run exits 0 and replaces all numbered output views. The failed diagnostic is kept outside the student packet.

This is a faithful task-card view, not a world-playthrough. It does not certify that narrative claims about world scenery, combat or off-card actions match gameplay. The initially recorded class-photo card exposed an older cage depiction. Before blind release, its actual CardShell binding was corrected to the new registered photo frame and the full class photo. Only card 061 was re-rendered; the other 102 screenshots are byte-identical (photo-packet-delta-proof.json). The private renderer now resolves the photo captive from the actual arena entity, rather than checking only normal phases. See implementation-photo-card.md for the code and gate evidence.

Root-only files: root-mapping-and-keys.json (number→original full task and level context), packet.manifest.json (source and output hashes), rendered-view-map.json (capture text), private-data.json and render source/bundle/driver. These must not be passed to blind readers. The frozen task source hash matches the on-disk file at packet completion. The student directory has exactly 104 files: 103 PNGs plus the text list. A literal leak check rejects task IDs and JSON key/evidence/grounding metadata in that text list.

## Final addendum after independent A/B and follow-up readers

The current student directory contains 106 PNGs plus ANSICHTEN.md (107 files), still all 61 cards. The three additional images are the real replacement-choice steps for mistake cards 041, 042 and 048. All 10 restore second steps remain included. Notebook cards 029/030 now have truly countable authored pictures; desk 055 has the registered wide desk and precise German Schreibtisch wording. Card 042 says In diesem Satz ist ein Fehler. No English key changed.

Current task SHA-256: c2dce3cfd76a7188ce1f9cf2cb8512209e8a2bdc3a8b91aed59ee59f48f264eb. packet.manifest.json, private root mapping and captured text are current. The final language-only re-record changes exactly 042-01/02 and 055-01/02; 102 other PNGs remain byte-identical (language-precision-packet-delta-proof.json). Earlier photo and notebook/desk hash proofs describe their historical stages, not the final full sequence. All four blind student directories passed the literal task-ID/key-metadata leak check.

Independent answers have now been audited by the author against keys outside student directories. See implementation-heft-blind-followup.md for every original A/B defect and its resolution, all 13 C/D/F follow-up answers, four E language judgments, the accepted Hi alternative, and limitations. The original sentence “No answer choices were solved or judged for this report” refers only to packet construction before independent review.
