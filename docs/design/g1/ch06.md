# ch06 · Detektiv — production sheet

*Status: TO BUILD (W3 wave, Act 2). The act-2 opener — the erasing goes explicit here.*

## 1 · Identity
- Building: **Detektiv / Detective** — Mo's office on a noir alley corner, long shadows
  (bible §3 hub table).
- Level: **Die verwischten Spuren** (`g1-ch06`, theme `detective`).
- Guardian: **Der Spuren-Verwischer** — a smudge that hides Mo's clues; shell-game shuffle
  (bible §3).
- Unit: `g1-u06` (49 vocab · structures: `a-lot-of` (24) · `present-simple` (30)).
  ⚠ Bible cell says "present simple questions" — u06 has no questions structure; corpus wins.
- EGA signature pair: dark gray `#545454`/black `#000000` alley × yellow `#fcfc54` lamplight
  pools; clue accents white `#fcfcfc` glint.

## 2 · Story spine
- Scenes: `s001–s010` (10; Mo speaks s002/s004/s007/s008).
- Anchors (world.json `beats`): door = `s001` · restore = `s006` (the recap scene).
- Task-slot scenes: s003 (name-it → `g1u06.w.detective`) · s004 (name-it → `g1u06.w.office`) ·
  s005 (fix-it → `g1u06.gi.present-simple.mc.001`) · s006 (recap → `g1u06.ci.mo-loss.mc.001`).
- Act-2 escalation: s007–s008 — Jona's note read aloud for the first time („Niemand hilft
  mir. Niemand spielt mit mir."); s009–s010 = Pixel hears something, Finn steadies the player.

## 3 · CLT block
- **Warum-Zeile**: „Jemand hat Mos Hinweise wegradiert — ohne sie kann der Detektiv niemandem
  mehr helfen. Mit Sätzen wie „Mo looks for clues" bringst du jede Spur zurück!"
- Task-reason table:

| where | task | unit content | the story reason |
|---|---|---|---|
| beat s003/s004 | name-it detective/office | job + place vocab | Mo cannot work without the word for what he IS and where |
| beat s005 | fix-it present-simple | `g1u06.s.present-simple` | the case file must SAY what Mo does every day |
| beat s006 | recap comprehension | `mo-loss` | retelling the case to Mo IS the restoration |
| level quickfires | detective + city vocab | vocab field | every sending carries an erased clue-word |
| wing tasks (seals) | a-lot-of | `g1u06.s.a-lot-of` | the clue pockets hold a lot of / lots of evidence — the guards make you count it |
| duel windows | a-lot-of · present-simple | both structures | every shuffle hides a clue under a wrong sentence about Mo's day |

## 4 · Level design (build spec — cookbook §7 archetype "dark maze box")
- Size ~55×35 (3.7×3.2 screens), boxy interior maze — the shape pivot after ch05.
- Flow: alley approach (cobble slopes, lamppost pole) → office door `1` → stacked office
  floors linked by drainpipe poles + `=` hatches → **2 clue-pocket wings via door pairs**
  (Aktenraum, walker guard, seal 1 · Archiv, hopper guard, seal 2) → optional third clue
  pocket (letter-rich, no seal) → rooftop skylight run (mover: window-washer lift over the
  alley gap) → the smudged door (boss).
- **Lamplight NOTE (for the W-wave to design):** darkness = VISIBILITY POCKETS, not real
  darkness — warm yellow lamplight islands over a dim-gray base (register: the unlit maze
  reads leer/still, never black-as-threat). Outside a pocket, props/letters render as pale
  outlines; hazards and guards stay fully readable; Glühwörter glow through. A render pass,
  not level data.
- Verbs: slopes (alley cobbles) · poles (lamppost + drainpipes, one threading a hatch) ·
  door pairs per clue pocket · 1 mover with a job (lift across the fall-recovery shaft) ·
  drop-through floorboards.
- Population (E, ≤1/screen): thief (Wortdieb — a clue-stealer, on-theme) ×1 · walker ×1 ·
  **Der Radierer** ×1 (NEW, doc 27 §3, introduced HERE: s001's „wegradiert" made a creature —
  erases the tiles ahead, re-write ~3s) · guards: walker (Aktenraum), hopper (Archiv). M adds
  the signature sending + flyer; S adds a second thief near the letter pocket.
- Signature sending: **Akten-Mimrock** — a filing-box "prop" that creeps only while
  unobserved (Mimrock grammar, cookbook §7): fake props among real ones.
- Collectibles: letters strung between lamplight pockets · 3 Glühwörter (archive top shelf,
  rooftop ledge, secret alcove) · helpers ×2 E.
- Secret: one bookcase is a fake prop — walk-in behind it → hidden evidence alcove
  (Glühwort) → drainpipe pole out.

## 5 · Boss duel (author `ch06.boss.json` to this)
- Name: **Der Spuren-Verwischer**. Intro: „Ein Wischfleck, der jede Spur versteckt. Er spielt
  sein Hütchenspiel mit Mos Fall." Outro: „Der Fleck hebt sich — jede Spur liegt wieder an
  ihrem Platz. Die Seite atmet auf." Taunts: „Wo ist es? Falsch geraten!" · „Meine Spuren!
  Alle meine!" · „Verwischt ist verschwunden."
- 5 knots · pattern [2,0,1,2,0,1] · telegraph E950/M780/S620 (shell-game tell: three smudge-
  cups slide and re-slide before the lane commits) · windows E25/M20/S15.
- Task mix: a-lot-of + present-simple (the unit's REAL structures — corpus overrides the
  bible cell), alternated with detective-vocab production. CLT: every knot = a shuffled cup
  holding a clue under a wrong sentence — the right form lifts the right cup.

## 6 · Assets (act-2 commission section)
- Level: `ch06_bg_far` (noir alley skyline, lamplit windows, loopable) · `ch06_bg_mid`
  (fire-escape + lamppost silhouette strip, transparent).
- New creature: `creature_radierer` (Der Radierer, 2-pose magenta sheet — doc 27 §3's act-2
  roster; canon pins no chapter, assigned here: ch06 is where the erasing is first spoken).
- Guardian: `boss_ch06_head_idle` · `boss_ch06_head_tell` · `boss_ch06_card` (clue cards) ·
  `boss_ch06_burst`.
- Map: `building_ch06` (Mo's office + lamppost corner, transparent 256).
- Beats: 1 illustration `ch06_beat_pinnwand` (Mo's clue board re-pinning itself, the strange
  paper at its center — restore beat, full-bleed).

## 7 · Beats staging
- Door beat s001–s005: Finn portrait; Mo portrait enters s002.
- Restore beat s006–s010: Finn + Mo portraits (Pixel in s009 has no speaking portrait);
  `ch06_beat_pinnwand` illustrates s006 — the note stays inside it, not as its own beat.
