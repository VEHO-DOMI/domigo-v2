# CODEX DRAFT — NOT CANON

## Clean devices and narrow portrait agreement

Two complete source objects approved by Root are now installed, replacing the old paper halos in both the world/object and ceremony fallback paths. The two source PNGs are unchanged and hashed in originals.manifest.json; Root owns the generation prompts in this pack. Both use proportional 512×512 registration, the full measured foreground plus six source pixels of margin, standard magenta removal and the existing fringe/speck cleanup. No new threshold, matte mode, raster drawing or material recolouring was introduced.

Installed aliases: obj_soundsystem and captive_soundsystem share one speaker image; obj_tablet and captive_tablet share one tablet image. Speaker opaque-art bounds are x64 y5 w384 h502; tablet x32 y5 w448 h502. The registration preserves each complete source silhouette. Source and output remaining fringe and specks are zero for all four. See import-report.json, installed.manifest.json and import.log. The unchanged device shell/window registration remains 820×736 with safe inset window x158 y149 w239 h405.

The task gate previously rejected the new device_locker_a portrait because the routing skin remains satchel. The added contract does not exempt satchel cards generally. It accepts only a chapter-one rescue/choice/state-it with exactly that skin, a closed device_locker_a/b art cell, and exactly one real serving entity in the authored phase. That entity must be a cage with shellArt=device_locker, no classmate, and the exact sound-system/tablet captive. The taught exercise, English naming answer and available obj_ artwork must all agree. Ordinary foreign portraits still fail their prior rule.

Validation: full check-game-tasks exits 0, 199 tasks across six files green. Existing draft-chapter findings remain reported as before. The selftest exits 0 including 16 new device cases: two real positives and fourteen negatives covering swapped captive, missing/other shell, wrong role/phase, ambiguous unscoped/duplicate route, wrong answer/exercise, person, open shell, wrong chapter, ordinary foreign portrait and missing actual device art. The deliberate lab-only gate mutation replacing shellArt agreement by false exits 1 specifically because missing-shell and other-shell controls no longer bite. The actual gate remains unmodified by this tamper. Logs are in CH01_STORY_SPIELPASS/art-devices-clean.

No engine, routing, schema, task or animation edits in this lane. A static naming depiction is not a world playthrough; Root owns final in-level review.

After installation, the actual picture-window and portrait tests pass: 26 tests across two files. The window tests read the installed PNGs and verify proportional containment. Log: art-devices-clean/picture-tests.log.
