# THE ART SHOPPING LIST v2 — re-cut for the Keen shape (2026-07-16)

> **2026-07-19 — Keen-era (v2), superseded in register by doc 31 §2 (STYLE_PAINT_V1):** v3 is
> authored with Batch Z; the pipeline laws (style-key first, one image proven end-to-end,
> manifests as allowlists) carry unchanged.

*Fable 5. Status: DOCUMENT — S2's input, superseding v1 (same day; the Keen-shape pivot, bible
= `docs/handover/27_the_book_and_the_school.md`). Pipeline laws unchanged: generate → drop
folder → `prep-art.mjs` QA → `sync-art.mjs` → `public/art/<ns>/`; manifests are allowlists;
**`_style_key.png` first, always; ONE image proven end-to-end in S2 before any volume.**
**New law (master plan W5): no batch for a surface whose engine slot isn't built** — the
vertical slice (GM-V1) freezes the year-1 slot list; this file names the families now and goes
stem-exact per batch as slots land.*

**v1 sections superseded:** the g1-school room tileset (23-stem manifest `g1-art-files.json`
stays on disk as reference; the top-down ROOMS retire — surviving stems noted below) and the
Spill per-room drops (its manifest was never created; doc 22's props re-enter as level furniture
in year 2). The DOM/detective manifest (`g2-art-files.json`, 126 stems) is untouched (LOOK-4).

## §1 · Now (S2 can execute immediately)

| batch | stems | notes |
|---|---|---|
| 1 · the style key | `_style_key` | the FireRed-48px ink-world cohesion reference; Koki gates it before anything else |
| 2 · pipeline proof | any ONE tile from batch 3 | drop → prep → sync → rendered screenshot vs key |
| 3 · characters (after GM-D3a/LOOK-0 cast pass) | `finn_down` `pixel_down` (hub sprites; survive from v1) + `jona_watch` (the map-edge figure) | player sprite stays procedural (unlockable-themed, bible §5.4) — NO player sheet |
| 4 · arcade wave-1 creatures (slots LIVE in K-3 today) | `tintenlaeufer` `huepfer` `flatterklecks` `wortdieb` `sprungkissen` `schattenwolke` (3–6 frames + `_burst` each) | readable at 48px in motion |
| 5 · economy + map furniture (slots land with GM-V1) | `flag` (static 4-frame + thrown-arc sheet) · `gluehwort` · `letter` glyph set · `funke` HUD icon · `note_pickup` | generate once the slice's slots exist |

## §2 · Year-1 waves (per act, AFTER the slice freezes the template)

Per chapter (15×): building sprite + ground tile family (bible §3 table: schoolhouse/inkwash
meadow · zoo gate/savanna · beached ship/cove … door home/spine bridge) · level tileset family
(themed solids/one-ways/hazards on the ink families) · guardian sheet (the bible §3 identity
table — telegraph frames are the load-bearing art: the tell must read in ~0.5s) · 1–2 beat
illustrations. New creatures ride their act: `radierer` `rotstift` `flatterbuchstaben` (wave
W3b). Ch15: Jona duel frames (sad-not-scary discipline — doc 18 §5 register binds art too).
Stem-exact tables are authored INTO each act's brief when the act wave opens.

## §3 · Year-2 waves (after W4 opens)

School hub map tiles (three bands + doc 22 §2.5 palette arc steps) · 15 school level families
(doc 22 §3 props as furniture; pale/taken props keep the outline+20% Blank signature) · leak
set-piece sheets · the Blank (absence-rendering: pale masks, never a creature until `klecks`).

## §4 · DOM games (unchanged from v1)

`g2-art-files.json` 126 stems (44 characters · 14 settings · 34 story beats · 17 props ·
17 cover/cards); first action stays wiring the 8 existing `*_ref` portraits (LOOK-4).

## §5 · Batch order (the law)

1 style key → 2 one-image proof → 3 characters (post-LOOK-0) → 4 arcade creatures → 5 economy/
map furniture (post-slice) → per-act year-1 batches following the content waves → year-2.
Per-batch ritual unchanged: key referenced in every prompt → prep QA → sync → in-game screenshot
vs key → Koki glance → manifest updated in the same PR.
